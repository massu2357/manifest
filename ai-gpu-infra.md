オンプレAI基盤は「層」で捉えると迷子になりません。まず全体像、その次にカリキュラムを提示します。

## 🗺️ 全体像：オンプレAI基盤の9層

```
⑨ ガバナンス   認証・監査・閉域（エアギャップ）
⑧ 運用         監視 / ログ / IaC / CI/CD / バックアップ
⑦ 推論基盤     vLLM・TensorRT-LLM・APIゲートウェイ
⑥ 統合管理     Kubernetes / GPU Operator
⑤ コンテナ     Docker・NVIDIA Container Toolkit・レジストリ
④ OS・ドライバ Linux / NVIDIA Driver / CUDA
③ ストレージ   NVMe・NFS・S3互換オブジェクト
② ネットワーク L2/L3・InfiniBand / RoCE
① 物理         電力・冷却・ラック・GPUサーバ選定
```

📌 **学習順序のコツ**：下から順に積むと退屈で挫折します。**④→⑤→⑦（まず動かす）→⑥→②③（あとから深掘り）** が続きやすい順路です。

---

## 📚 カリキュラム（週5〜10時間想定）

### Phase 0｜Linux・ネットワークの土台（2〜3週）
- systemd、パッケージ管理、`journalctl`、リソース確認コマンド
- IP/サブネット、ルーティング、DNS、netplan、MTU、プロキシ設定
- ✅ **完了判定**：静的IP＋DNS＋プロキシ設定済みのUbuntuサーバを、手順書付きで1台構築できる

### Phase 1｜GPUを動かす層（2週）
- **Driver / CUDA Toolkit / Fabric Manager** の役割の違いを説明できる
- `nvidia-smi` の読み方（利用率・VRAM・ECC・電力・`topo`）
- Docker ＋ **NVIDIA Container Toolkit**（コンテナにGPUを見せる仕組み）
- 用語整理：**MIG**＝1枚のGPUを物理分割 / **time-slicing**＝時分割で共有 / **MPS**＝プロセス並行実行
- ✅ **完了判定**：コンテナ内で `nvidia-smi` とCUDAサンプルが動く

### Phase 2｜1台でLLM推論基盤を作る（3〜4週）🎯 ここで一気に面白くなる
- vLLM で **OpenAI互換API** を起動
- モデル形式（safetensors / GGUF）と量子化（FP16・FP8・AWQ）の関係
- **VRAM見積り**：パラメータ数 × 精度 ＋ KVキャッシュ（同時実行数×文脈長で膨らむ部分）
- 性能指標：スループット（tok/s）、**TTFT**（最初の1文字が返るまでの時間）、同時実行数
- ゲートウェイ（LiteLLM等）とUI（Open WebUI）で「使える形」にする
- ✅ **完了判定**：モデルサイズからVRAM必要量を概算し、実測と突き合わせられる

### Phase 3｜Kubernetes（4〜8週）⛰️ 最大の山
| 段階 | 内容 |
|---|---|
| 3-1 基礎 | Pod / Deployment / Service / ConfigMap / Secret、`kubectl` |
| 3-2 ストレージ | PV / PVC / StorageClass → **CSI**（外部ストレージをk8sに繋ぐ標準規格）。モデル置き場をNFSやオブジェクトで用意 |
| 3-3 ネットワーク | **CNI**（Pod間通信の実装：Calico / Cilium）、Service / Ingress、内部DNS |
| 3-4 GPU | **NVIDIA GPU Operator**、Device Plugin、`nvidia.com/gpu` によるリソース要求、MIG・time-slicingでの配分 |

- 💡 学習目的ならマネージドではなく **kubeadmで自前構築**。中身が見えます
- ✅ **完了判定**：k8s上のvLLM Podに任意のGPU割当ができ、モデルはPVC経由で読める

### Phase 4｜マルチノード・分散（3〜4週）
- **NCCL**（GPU間の集団通信ライブラリ）、`NCCL_*` 環境変数とトポロジ確認
- **InfiniBand / RoCE / RDMA**（CPUを介さない高速転送）、MTU、GPUDirect
- **テンソル並列(TP)**＝1層を複数GPUで分割 / **パイプライン並列(PP)**＝層ごとに分担
- ✅ **完了判定**：2ノード構成でモデルが動き、単ノードとの性能差を説明できる

### Phase 5｜運用（以降は継続）
- 監視：Prometheus ＋ Grafana ＋ **DCGM Exporter**（GPUメトリクス収集の定番）
- ログ基盤、**Ansible**による構成管理、CI/CDへの接続
- キャパシティプランニング（電力・冷却・ラック密度の制約込み）

### Phase 6｜オンプレ／閉域ならではの領域（継続・実務価値が最も高い）
- **エアギャップ運用**：ミラーレジストリ（Harbor）、OSリポジトリミラー、モデル持ち込み手順
- 内部CA・証明書・プロキシ設計
- OIDC / RBAC によるテナント分離、監査ログ、モデルとデータのライフサイクル管理

---

## 🔑 優先度の目安

| 分野 | 重要度 | 着手 |
|---|---|---|
| Linux / ネットワーク基礎 | ★★★ | 最初 |
| GPUドライバ・コンテナGPU | ★★★ | 早期 |
| 推論エンジン（vLLM） | ★★★ | 早期 |
| Kubernetes（特にCSI・GPU割当） | ★★★ | 中盤 |
| 監視・IaC | ★★☆ | 中盤以降 |
| 分散通信（NCCL / RoCE） | ★★☆ | 後半 |
| 物理・電力・冷却 | ★☆☆ | 必要時に都度 |

## 💡 続けるためのルール
- **1フェーズ＝1成果物**（手順書 or ベンチ結果）にすると、理解の穴がすぐ見つかります
- k8sは「作って壊す」を最低3回。1回目は必ず動かず、それで正常です
- 検証環境は1台から始めて十分（Phase 0〜3は単一ノードで通せます）

これを週割りの具体プランに落とすため、2点だけ確認させてください。

「両方＋時間未定」なら、**週割りではなく「コマ単位のマイルストーン設計」**にするのが正解です。時間が決まった時点で、掛け算するだけでスケジュール化できます。

## ⏱️ 1コマ＝90分換算の所要量

| Phase | 内容 | 目安コマ数 |
|---|---|---|
| 0 | Linux・ネットワーク土台 | 8〜10 |
| 1 | GPUドライバ・コンテナGPU | 6〜8 |
| 2 | vLLMで推論基盤（単ノード） | 12〜16 |
| 3 | Kubernetes（最大の山） | 24〜32 |
| 4 | マルチノード・分散 | 10〜14 |
| 5 | 運用・監視・IaC | 継続（月2〜4） |
| 6 | 閉域・ガバナンス | 継続（月2〜4） |

**合計 約70〜90コマ**（Phase 0〜4）

- 週2コマ（3時間）→ 約9ヶ月
- 週5コマ（7.5時間）→ 約4ヶ月
- 週10コマ → 約2ヶ月

📌 時間が読めないうちは「今週何時間」ではなく **「今どのコマにいるか」** で管理すると、止まっても再開できます。

---

## 🔄 自分＋展開の二層構造：「1フェーズ先行」方式

自分とチームを同時に走らせず、**自分が1フェーズ先を行く**形にするのが最も負荷が低いです。

```
自分:   Phase 1 ──▶ Phase 2 ──▶ Phase 3 ──▶ Phase 4
              ↓教材化      ↓教材化      ↓教材化
チーム:      Phase 1 ──▶ Phase 2 ──▶ Phase 3
```

✅ **メリット**
- 教材が「実際に自分がハマった箇所」ベースになり、机上の資料より圧倒的に実用的
- 教える行為が自分の理解の穴を炙り出す（説明できない＝理解していない）
- 学習と成果物づくりが一度で済む

---

## 🎯 ロール別の到達ライン（全員が Phase 4 まで行く必要はない）

| 対象 | 到達ライン | 狙い |
|---|---|---|
| **AI利用者・アプリ開発者** | Phase 0〜2 | APIを叩く側として、VRAMや同時実行数の制約を理解している |
| **運用担当** | ＋ Phase 3・5 | 障害切り分けとGPU割当変更ができる |
| **基盤設計担当** | ＋ Phase 4・6 | 構成選定・閉域設計まで判断できる |

💡 チーム展開では **Phase 2 までを「全員必修」** に切り出すのが現実的です。ここまでで「AI基盤の会話が通じる人」になります。

---

## 📦 各フェーズの教材化3点セット

フェーズ終了時に、この3つだけ残します。作りすぎると続きません。

| 成果物 | 内容 | 所要 |
|---|---|---|
| **① 手順書** | コピペで同じ環境が再現できる粒度 | 1コマ |
| **② ハンズオン課題** | 「壊れた状態」を渡して直させる形式が効果的 | 0.5コマ |
| **③ 確認テスト** | 5問程度。用語の暗記ではなく判断を問う | 0.5コマ |

**③の例（Phase 2）**：「70Bモデルを FP16 で動かしたい。H200×1枚で足りるか、根拠と共に答えよ」

このテストは自分の理解度チェックとチーム教材を兼ねるので、作る価値が二重にあります。

---

## 👉 最初の1コマ

**「検証用Ubuntuサーバを1台、静的IP＋プロキシ設定込みで構築し、手順書を書く」**

ここでいきなり詰まるポイント（DNS、プロキシ除外設定、netplanの反映）が、そのままチーム教材の第1章になります。

Phase 0 の手順書テンプレートと確認テストのたたき台を用意することもできますので、必要でしたらお知らせください。