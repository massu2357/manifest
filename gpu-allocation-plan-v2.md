# GPU基盤 モデル配置・ユースケース割り振り設計案（改訂版）

**対象構成**: NVIDIA B300 × 8（1台） / NVIDIA H200 × 4（1台）
**対象ユースケース**: 8分野・約50件
**想定同時実行数**: 最大100リクエスト（vLLMに到達する推論リクエスト単位）
**モデル情報の基準日**: 2026年8月20日

---

## 0. 改訂の要点

初版からの主要な変更点は以下の3点である。

| 項目 | 初版 | 改訂版 |
| --- | --- | --- |
| 同時実行数の前提 | 1,000〜1,300（推定） | **100（確定値・推論リクエスト単位）** |
| 設計の主眼 | スループット（処理量）の確保 | **品質・コンテキスト長・レイテンシへの投資** |
| モデル選定 | 「671B級」等の抽象表現 | **2026年8月時点の実在モデル名で確定** |

同時実行数が一桁下がったことで、本構成は**処理量の観点では大幅な余裕を持つ**。この余剰を放置せず、モデル品質・コンテキスト長・応答速度・冗長性へ意図的に振り向けることが本改訂の主題である。

---

## 1. 確定した前提条件

### 1.1 同時実行数

最大100リクエスト。人単位ではなく、vLLMへ到達する推論リクエスト単位での数値である。

これにより、エージェント処理やRAGによる呼び出し回数の増幅（1ユーザー操作が3〜30回のLLM呼び出しに展開される現象）は既に織り込み済みと解釈できる。

### 1.2 同時実行数だけでは容量が決まらない点

リクエスト件数と同等以上に、**コンテキスト長**が容量を左右する。

> **コンテキスト長とは**
> 1回のリクエストでモデルが同時に扱えるトークン（単語の断片）の総量である。入力と出力の合計に適用される。128Kは約12万8千トークンを指す。

128Kトークンのリクエスト1件は、8Kトークンのリクエスト16件分のKVキャッシュを消費する。したがって、容量設計は「同時100件」ではなく「**確保可能なKVトークン総量**」で行う。

> **KVキャッシュとは**
> LLMが文章を生成する際、それまでに読んだ／書いたトークンの中間計算結果を保持しておく領域である。GPUメモリのうち、モデルの重みを除いた残り全てがここに充てられる。同時接続数とコンテキスト長の上限を直接決定する。

### 1.3 GPUメモリの内訳（原則）

```
必要GPUメモリ ＝ ①モデルの重み ＋ ②KVキャッシュ ＋ ③作業領域（10〜20%）
```

実務上の指針として、**①の重みは総メモリの50〜60%以下に抑える**。これを超えると②が枯渇し、同時実行数またはコンテキスト長のいずれかを大幅に削らざるを得なくなる。

---

## 2. 2026年8月時点のローカルモデル選択肢

### 2.1 サイズ別一覧

| 階層 | モデル名 | 開発元 | 総パラメータ／活性 | ライセンス | 備考 |
| --- | --- | --- | --- | --- | --- |
| 2兆超 | **Kimi K3** | Moonshot AI | 2.8T / 104B | 独自（要確認） | 史上最大。1M文脈、画像・動画入力対応。HF配布は約1.56TB |
| | **Qwen3.8-Max** | Alibaba | 2.4T / 95B | 未確定 | 1M文脈、マルチモーダル。重み公開は2026年8月中旬予定 |
| 1〜2兆 | **DeepSeek V4 Pro** | DeepSeek | 1.6T / 49B | MIT | 1M文脈。ハイブリッドアテンション |
| | **LongCat 2.0** | Meituan | 1.6T | MIT | 中国製チップで学習 |
| | **Kimi K2.7 / K2.6** | Moonshot AI | 約1T / 32B | 独自 | K3の前世代 |
| | **Ling 2.5 / Ring 2.5** | Ant Group | 1T | — | ハイブリッドアテンション |
| 500B〜1T | **Inkling** | Thinking Machines Lab | 975B | — | 米国系では例外的な大型 |
| | **GLM-5.2** | Z.ai（智譜） | 744B / 40B | **MIT** | オープンウェイト首位級。SWE-bench Pro 62.1% |
| | **Nemotron 3 Ultra** | NVIDIA | 561B | オープン | 学習レシピ・データも公開 |
| 100〜500B | **Qwen3.5-397B-A17B** | Alibaba | 397B / 17B | Apache 2.0 | |
| | **DeepSeek V4 Flash** | DeepSeek | 284B / 13B | MIT | 1M文脈。活性パラメータが少なく高速 |
| | **Inkling-Small** | Thinking Machines Lab | 276B / 12B | — | |
| | **MiniMax M3** | MiniMax | 非公開 | オープン | パラメータ数未開示 |
| | **Ling-3.0-flash** | Ant Group | 124B / 5.1B | — | |
| | **Nemotron 3 Super** | NVIDIA | 120B / 12B | オープン | 1M文脈。全学習パイプライン公開 |
| 100B未満 | **Qwen3.6-27B / 3.8-27B** | Alibaba | 27B（密） | Apache 2.0 | 1GPUで動作 |
| | **Mistral Small 24B Instruct** | Mistral AI | 24B（密） | Apache 2.0 | GitLab Duo全機能に完全対応 |
| | **Codestral 22B** | Mistral AI | 22B（密） | 独自 | GitLab Duoのコード補完・生成に完全対応 |

> **MoE（Mixture of Experts / 混合エキスパート）とは**
> モデル内部を多数の「専門家（エキスパート）」に分割し、入力に応じてごく一部だけを動作させる構造である。上表の「総パラメータ／活性」のうち、**総パラメータが必要GPUメモリを決め、活性パラメータが生成速度を決める**。
> 例としてKimi K3は総2.8兆パラメータを持つが、1トークンあたり896個のエキスパートのうち16個しか動作しない。「パラメータ数が大きい＝遅い」は成立しない。

> **密（Dense）モデルとは**
> MoEと対比される従来型の構造で、全パラメータが常に動作する。総パラメータ数と活性パラメータ数が一致する。小型モデルの多くはこちらである。

### 2.2 必要GPU枚数への換算

FP8精度（1パラメータ = 1バイト）を前提とし、重みが総メモリの50〜60%以下に収まる枚数を示す。

> **FP8 / BF16 / FP4 とは**
> モデル内部の数値を何ビットで表現するかを示す精度指定である。BF16は1パラメータあたり2バイト、FP8は1バイト、FP4は0.5バイトを消費する。ビット数を下げると必要メモリが減る代わりに、わずかな精度低下が生じうる。これを**量子化**と呼ぶ。B300世代はFP4をハードウェアで直接サポートするため、さらなる圧縮の余地がある。

**B300（1枚あたり288GB）の場合**

| 必要枚数 | 該当モデル | 重み | 残KV容量 |
| --- | --- | --- | --- |
| **8枚**（2,304GB） | Kimi K3 | 約1,560GB | 約744GB |
| | DeepSeek V4 Pro | 約1,600GB | 約704GB |
| | Qwen3.8-Max（4bit時） | 約1,200GB | 約1,100GB |
| **6枚**（1,728GB） | Inkling | 約975GB | 約753GB |
| **4枚**（1,152GB） | **GLM-5.2** | 約744GB | 約408GB |
| | Nemotron 3 Ultra | 約561GB | 約591GB |
| **2枚**（576GB） | Qwen3.5-397B | 約397GB | 約179GB |
| | **DeepSeek V4 Flash** | 約284GB | 約292GB |
| **1枚**（288GB） | Nemotron 3 Super / Ling-3.0-flash | 約120GB | 約168GB |
| | Mistral Small 24B / Codestral 22B | 約24GB | 約264GB（複数レプリカ可） |

**H200（1枚あたり141GB / 4枚で564GB）の場合**

| 必要枚数 | 該当モデル | 判定 |
| --- | --- | --- |
| 4枚（564GB） | Qwen3.5-397B | 動作するがKV約167GBと窮屈 |
| 3〜4枚 | DeepSeek V4 Flash（284B） | 4枚ならKV約280GBで快適 |
| 2枚（282GB） | Nemotron 3 Super（120B）級 | 快適 |
| 1枚（141GB） | 24B級、埋め込みモデル、拡散モデル | 快適 |

**H200×4の実質的な上限は400B級である。** GLM-5.2（744GB）はFP8では収まらない。

---

## 3. 割り振り設計

### 3.1 案A: バランス型（推奨）

品質・速度・冗長性・拡張性のすべてを確保する構成である。

**B300 × 8（2,304GB）**

| プール | GPU | 構成 | 搭載モデル | 想定同時 | 役割 |
| --- | --- | --- | --- | --- | --- |
| **P1: 最高品質** | 0-3 | TP=4 | **GLM-5.2（744B）**<br>128Kコンテキスト | 20〜30 | 品質が要件を左右する用途 |
| **P2: 主力** | 4-5 | TP=2 | **DeepSeek V4 Flash（284B）** | 40〜50 | 日常業務の大半 |
| **P3: 低遅延** | 6 | TP=1 × 複数 | **Mistral Small 24B**<br>**Codestral 22B** | 20〜30 | コード補完・GitLab Duo |
| **P4: 学習・評価** | 7 | — | 可変 | — | 常設の改善サイクル |

**H200 × 4（564GB）**

| プール | GPU | 用途 |
| --- | --- | --- |
| **R1: 検索基盤** | 0 | 埋め込みモデル＋リランカー（常時稼働） |
| **R2: 画像生成** | 1 | 拡散モデル（FLUX / SDXL系）を専有 |
| **R3-4: 縮退・検証** | 2-3 | 平時は新モデルの評価。B300停止時は**DeepSeek V4 Flashで全用途を縮退運転** |

**採用理由**

- **P1にGLM-5.2を選ぶ理由**: MITライセンスであること、オープンウェイト首位級の実績があること、744GBという規模がB300の4枚（1,152GB）にちょうど収まりKVを408GB確保できること。品質・法務・容量の3点が同時に成立する唯一に近い選択肢である。
- **P2にDeepSeek V4 Flashを選ぶ理由**: 総284Bに対し活性パラメータが13Bと少なく、**284Bクラスの品質を13B相当の生成速度で得られる**。2枚でKVを292GB確保でき、日常業務の主力として最も費用対効果が高い。
- **P3を分離する理由**: コード補完は1秒以内の応答が要件であり、長文処理に巻き込まれてはならない。またGitLab Duoは対応モデルが限定されており（第4.1項）、上位プールのモデルを共用できない。

### 3.2 案B: 最大モデル集中型

Kimi K3（2.8T）またはDeepSeek V4 Pro（1.6T）を投入する構成である。いずれもB300の8枚全てを占有する。

**B300 × 8（2,304GB）**

| プール | GPU | 構成 | 搭載モデル |
| --- | --- | --- | --- |
| **単一プール** | 0-7 | TP=8 | **Kimi K3（2.8T）** または **DeepSeek V4 Pro（1.6T）** |

**H200 × 4（564GB）** — 周辺機能を全て引き受ける

| プール | GPU | 用途 |
| --- | --- | --- |
| R1 | 0 | 埋め込み＋リランカー |
| R2 | 1 | 画像生成 |
| R3 | 2 | **Codestral 22B / Mistral Small 24B**（GitLab Duo・コード補完） |
| R4 | 3 | 検証・評価 |

**得られるもの**

- 現存する最高品質のオープンウェイトモデルを社内で利用できる
- 1Mトークンという極めて長いコンテキストを扱える

**失うもの**

| 失う要素 | 影響 |
| --- | --- |
| 冗長性 | B300が停止すると、コード補完以外の全ユースケースが停止する |
| 学習・評価枠 | 継続的な改善サイクルを回す場所がなくなる |
| 中量級プール | 単純な要約や分類にも2.8兆パラメータのモデルを使うことになり、応答が不必要に遅くなる |
| ライセンスの自由度 | Kimi K3はMITではなく独自ライセンスであり、法務確認が必須 |

### 3.3 比較と推奨

| 観点 | 案A: バランス型 | 案B: 最大モデル集中型 |
| --- | --- | --- |
| 最高品質 | 744B級 | **2.8T級** |
| 応答速度（一般業務） | **良好**（用途別に最適サイズ） | 劣る（全て最大モデル経由） |
| 冗長性 | **あり**（H200で縮退可能） | ほぼなし |
| 学習・評価 | **常設枠あり** | なし |
| ライセンス | **MIT中心で明快** | 要法務確認 |
| 運用の複雑さ | 中（4エンドポイント） | 低（1エンドポイント） |

**結論として案Aを推奨する。**

案Bの最大モデルは、案AのP4枠（GPU 7）では容量が足りず検証できない。したがって、最大モデルの評価が必要な場合は**期間を区切ってB300全体を案B構成に切り替え、評価後に案Aへ戻す**という運用が現実的である。詳細は第6章に記す。

---

## 4. ユースケース50件のマッピング

### 4.1 カテゴリ別の割り当て（案A前提）

| # | カテゴリ | 割当先 | レイテンシ / 品質 | 補足 |
| --- | --- | --- | --- | --- |
| 1 | レポート生成 | **P1** | P1 / 高 | 長文出力。品質が成果物の価値を直接左右する |
| 2 | 外部情報収集 | **P2**（夜間はP4） | P2 / 中 | 索引化はR1。バッチ化しやすく最優先で夜間へ回す |
| 3 | 情報集約・要約 | **P2** | P1-P2 / 中 | 入力が長くPrefill重め。定期実行分はP4へ |
| 4 | データ分析（回帰・分類） | **R3** ＋ **P2** | P2 / 中 | 第5.2項の解釈確認が前提 |
| 5 | コード生成支援・解析支援 | 補完 → **P3**<br>解析・レビュー → **P1** | P0 / 小・高 | 性質が正反対のため必ず分離する |
| 6 | 内部ドキュメントRAG | 検索 → **R1**<br>生成 → **P2**（高精度時 **P1**） | P0-P1 / 中 | 最も利用が伸びやすい。要監視 |
| 7 | まとめ生成＋画像作成 | 文章 → **P2**<br>画像 → **R2** | P1 / 中 | 画像生成に数十秒を要するため非同期処理を前提とする |
| 8 | 内部GitLabデータ操作（GitLab Duo） | 補完 → **P3**<br>Chat → **P3** | P0 / 小 | 下記の制約に注意 |

> **Prefill と Decode とは**
> LLMの推論は2段階に分かれる。**Prefill**は入力プロンプト全体を読み込む段階で、演算性能が律速となる。**Decode**は1トークンずつ生成する段階で、メモリ帯域が律速となる。
> RAGや長文要約は入力が長いためPrefill重め、対話やエージェント処理は出力が長いためDecode重めとなり、効いてくるハードウェア性能が異なる。

**GitLab Duo に関する重要な制約**

GitLab Duo Self-Hostedは対応モデルの一覧が定められており、機能ごとに完全対応・部分対応・非対応が区分されている。例として、Codestral 22Bはコード補完・コード生成に完全対応する一方でChatは対象外、Mistral Small 24B Instructは3機能すべてに完全対応する。

**したがってDuoは、P1やP2の大規模モデルを共用できない。** P3に専用エンドポイントを立てる前提で設計する。配信基盤としてvLLMは公式サポート対象であり、エンドポイントURLは末尾に `/v1` を付与して指定する。着手順としては、公式の対応表で必要機能（補完／生成／Chat）を満たすモデルを確定させることが先行する。

### 4.2 「50件 ≠ 50モデル」の実像

50件のユースケースは、最終的に**6本のエンドポイント**へ集約される。

| エンドポイント | 想定件数 | 内容 |
| --- | --- | --- |
| P1（GLM-5.2） | 約10〜15件 | 品質が要件を左右する用途 |
| P2（DeepSeek V4 Flash） | 約25〜30件 | 日常業務の大半 |
| P3（24B級） | 数件（ただし高頻度） | コード補完・Duo |
| R1（埋め込み・リランカー） | — | RAG検索の前段 |
| R2（拡散モデル） | 数件 | 画像生成 |
| R3（機械学習） | 数件 | 回帰・分類 |

### 4.3 1つのモデルを複数用途で共有する仕組み

LLMの推論は**ステートレス**である。1リクエストごとに入力が完結して届き、処理後に何も残らないため、異なる用途のリクエストが混在しても互いに干渉しない。

用途ごとの差異は、GPUを分けることなく上位層で吸収する。

| 層 | 手段 | 例 |
| --- | --- | --- |
| アプリケーション層 | システムプロンプト | 「あなたは契約書レビューの専門家である」等の役割指定 |
| | 生成パラメータ | レポート生成は temperature 0.7 / max_tokens 8000、分類は temperature 0 / max_tokens 50 |
| | 出力形式の指定 | JSONスキーマを渡して構造化出力を強制 |
| | 入力コンテキスト | RAG検索結果、ソースコード、社内文書 |
| ルーティング層 | 優先度付与・レート制限・監査ログ | 用途識別子による分類 |
| 推論層（vLLM） | 変更なし | モデルは共通 |

vLLMは**継続バッチング**により、用途の異なるリクエストを自動的に同一バッチへまとめて処理する。

> **継続バッチング（Continuous Batching）とは**
> 従来の推論エンジンは「一定件数が溜まるまで待ってから一括処理」していたが、vLLMは処理が完了したリクエストの枠に次のリクエストを即座に差し込む。用途が混在してもGPU利用効率が低下しない。

**さらに用途特化が必要な場合**は、マルチLoRAという選択肢がある。

> **マルチLoRA とは**
> 1つのベースモデルに対し、用途特化の小さな追加パラメータ（LoRAアダプタ、各数GB程度）を複数同時にロードしておき、リクエストごとに切り替える仕組みである。GPUを追加せずに用途特化モデルを複数持てる。
> ただし大規模MoEモデルでの運用実績は限定的であるため、まずP2（284B級）で検証し、効果を確認してから上位へ展開することが現実的である。

---

## 5. 設計上の論点

### 5.1 ノイジーネイバー問題

同一エンドポイントを共有する以上、長大なリクエストが他を圧迫する。

| 状況 | 影響 |
| --- | --- |
| 128Kコンテキストのレポート生成が1件走る | 8Kのリクエスト16件分のKVキャッシュを占有する |
| 出力8,000トークンの生成が長時間居座る | バッチ枠を塞ぎ、後続の待ち時間が伸びる |

**対策**

1. vLLMの優先度スケジューリングを有効化し、リクエストに優先度を付与する
2. P0要件（コード補完）は**そもそも別エンドポイント（P3）へ物理分離する** — 本設計でP3を独立させている理由である

ソフトウェア的な優先度制御は、GPU演算資源の奪い合いに対しては十分に機能しない。確実性を求めるなら物理分離が唯一の解である。

### 5.2 「データ分析（回帰・分類）」の解釈確定

2通りの解釈が可能であり、必要リソースが一桁変わる。

| 解釈 | 必要なもの |
| --- | --- |
| A. 古典的な機械学習（XGBoost、scikit-learn等） | GPUはほぼ不要。CPUサーバで十分 |
| B. LLMに分析コードを書かせる／結果を解釈させる | P2で対応可能 |
| C. 深層学習モデルの学習（時系列・表形式ニューラルネット等） | R3を専有 |

**Aが大半であれば、本GPU基盤を割り当てるべきではない。** 早期の切り分けを推奨する。

### 5.3 起動時に固定されるパラメータ

以下はvLLMインスタンス単位で決定され、リクエストごとには変更できない。

| 項目 | 影響 |
| --- | --- |
| `--max-model-len`（最大コンテキスト長） | **最も長いコンテキストを要する用途に合わせる必要がある** |
| 量子化方式（FP8等） | 全用途で共通となる |
| LoRAの有効化と上限数 | 事前決定が必要 |

128Kを要する用途が1件でも存在すれば、インスタンスは128Kで起動することになる。短いリクエストが不利になるわけではないが、**メモリ設計はこの要件に引っ張られる**点に留意する。

### 5.4 セキュリティ境界の共有

同一インスタンスを共有する場合、異なる機密区分のリクエストが**同一プロセス・同一GPUメモリ上**で処理される。

| 論点 | 内容 |
| --- | --- |
| プレフィックスキャッシュ | 同一の入力冒頭を再利用する高速化機能。理論上、他リクエストの存在を推測できる余地がある |
| プロセス分離 | vLLM内部に機密区分の概念は存在しない |

機密区分をまたぐ用途を同一エンドポイントに載せる場合は、意図的な判断が必要である。分離が必要な場合の選択肢は次の2つで、それぞれ性能またはGPU枚数を犠牲にする。

1. プレフィックスキャッシュを無効化する（性能低下）
2. エンドポイント（GPUプール）自体を分ける（GPU消費増）

**機密区分の分離要件が確定するまで、本設計は暫定である。** 分離が必要な場合、B300の8枚を4+4に割るなど、構成が根本から変わる。

### 5.5 ルーティング層の設置

50個のアプリケーションがそれぞれモデル名とエンドポイントを直接記述する状態は、運用不能に陥る。

- 単一の入口（AI Gateway / LiteLLM / vLLM router 等）を設置する
- アプリケーション側は「用途名」のみを指定する（例: `summarize-fast`）
- 用途名からプールへのマッピングは基盤側で保持する
- **全リクエストに用途識別子を必ず付与する** — これがないと利用実態の把握もコスト按分も不可能になる

この構造により、モデルの入れ替え、A/B比較、利用量の可視化がすべて基盤側で完結する。

### 5.6 モデル選定にあたっての留意事項

**総パラメータ数は実力を表さない**

第2.1項のとおり、見るべきは「総パラメータ（必要メモリを決定）」と「活性パラメータ（生成速度を決定）」の両方である。片方だけでは判断できない。

**ベンチマークの多くはベンダー自己申告である**

特に最新の大規模モデルについては、独立した第三者検証が追いついていない。**自社の評価セットで測定するまでは、公開されている順位表を判断材料にしない。** 案AのP4枠およびH200のR4枠は、この検証を行うために確保している。

**上位モデルの開発元が偏在している**

2026年に入って以降、最大規模かつ最高性能のオープンウェイトモデルは中国系ラボから継続的に公開されている。米国系ラボのモデルは、NVIDIAのNemotron 3 Ultra（561B）等の例外を除き、多くの月で130B未満に留まっている。

重みが公開されエアギャップ環境で動作する以上、技術的リスクは限定的である。ただし**組織の調達規程やセキュリティ規程が開発元に制約を設けている場合があるため、事前確認を推奨する**。制約がある場合の代替はNemotron 3系（NVIDIA）、gpt-oss（OpenAI）、Mistral 3系となるが、規模は一段下がる。

**vLLMの対応時期にタイムラグがある**

新しいアーキテクチャ（Kimi K3のAttention Residuals、DeepSeek V4のハイブリッドアテンション等）は、vLLMが対応するまで数週間から数ヶ月を要する場合がある。**導入予定モデルは、必ずvLLMの対応状況を先に確認する。**

---

## 6. 導入ステップ

### フェーズ1: 前提の確定（着手前）

1. **機密区分の分離要件を確定する**（第5.4項） — これが未確定のまま構成を組むと手戻りが大きい
2. **「データ分析」の解釈を確定する**（第5.2項）
3. **GitLab Duoの必要機能と対応モデルを確定する**（第4.1項）
4. **50件のうち、128K級の長文コンテキストを要するものを洗い出す**

### フェーズ2: 検証（H200で実施）

5. 50件を「P1（高品質必須）／P2（中で十分）／P3（小型・低遅延）」に仕分ける
6. 各クラスの代表ユースケースを1件ずつ選び、H200のR4枠で実測する
7. 同時実行数を振ってTTFT / TPOTのカーブを取得する

> **TTFT / TPOT とは**
> **TTFT**（Time To First Token）は、リクエスト送信から最初の1文字が返るまでの時間である。体感速度を左右し、対話用途では1〜2秒以内が目安となる。
> **TPOT**（Time Per Output Token）は、1トークンを生成するのに要する時間である。文章が流れる速さに相当し、人間の読む速度である毎秒10〜20トークンが実用上の下限となる。
> いずれも平均値ではなく **p95**（95%のリクエストが満たすべき値）で目標設定する。

### フェーズ3: 本番構築

8. 案Aの構成でB300を立ち上げる。ただし**まず70%の配分で起動**し、残り30%は実利用データを見て調整する
9. ルーティング層を先に設置し、アプリケーションからの直接接続を許可しない

### フェーズ4: 継続改善

10. P4枠で新モデルの評価を継続する
11. **最大モデル（案B）の評価は期間を区切って実施する** — B300全体を一時的に案B構成へ切り替え、評価完了後に案Aへ戻す。これはP4枠（288GB）では容量が不足するためである

**机上計算のみで確定させないこと。** 特にMoEモデルは重みと実際の計算量の乖離が大きく、実測との差が出やすい。

---

## 7. 補足: レイテンシ最適化という選択肢

同時実行数が100件と限定的であることにより、通常のGPU基盤では選べない設定が可能になる。

**従来のセオリー**: モデルが載る最小のGPU枚数でTPを組む（スループット効率が最大）

**本構成で取りうる方針**: レイテンシが最速になる多めのGPU枚数でTPを組む

> **TP（テンソル並列 / Tensor Parallel）とは**
> 1つのモデルを複数のGPUへ横方向に分割し、分担して処理させる方式である。GPU間の通信量が非常に多いため、NVLink（GPU同士を直結する高速インターコネクト）で結ばれた同一サーバ内でのみ使用する。

トークン生成（Decode）はメモリ帯域が律速であるため、GPU枚数を増やすと帯域が合算され、**1リクエストあたりの生成速度が向上する**。

| 構成 | 単一リクエストの生成速度 | 演算効率 |
| --- | --- | --- |
| TP=1 | 基準 | 最高 |
| TP=2 | 約1.6〜1.8倍 | 中 |
| TP=4 | 約2.5〜3倍 | 低 |

同時実行数に余裕がある本構成では、**演算効率を犠牲にしてレイテンシを買う判断が合理的である**。特にコード補完（TTFT短縮が体感に直結）、レポート生成（長文出力の完了時間短縮）、エージェント処理（思考ループの各ステップが累積で効く）で効果が大きい。

---

## 8. 用語集

| 用語 | 説明 |
| --- | --- |
| KVキャッシュ | 生成途中の中間結果を保持する領域。同時接続数とコンテキスト長の上限を決める |
| コンテキスト長 | 1リクエストで扱えるトークン総量。入力と出力の合計に適用される |
| MoE（混合エキスパート） | 内部を専門家に分割し一部のみ動作させる構造。総パラメータがメモリを、活性パラメータが速度を決める |
| 密（Dense）モデル | 全パラメータが常に動作する従来型構造。総＝活性 |
| FP8 / BF16 / FP4 | 数値の表現精度。順にメモリ消費が半減していく（量子化） |
| TP（テンソル並列） | 1モデルを複数GPUへ横分割する方式。通信量が多くノード内限定 |
| PP（パイプライン並列） | 1モデルを層単位で縦分割する方式。通信量が少なくノード間向き |
| Prefill | 入力を読み込む段階。演算性能が律速 |
| Decode | 1トークンずつ生成する段階。メモリ帯域が律速 |
| TTFT | 最初の1文字が返るまでの時間。体感速度の指標 |
| TPOT | 1トークンあたりの生成時間。文章が流れる速さの指標 |
| 継続バッチング | 完了したリクエストの枠へ次を即座に差し込む方式。用途混在でも効率が落ちない |
| プレフィックスキャッシュ | 同一の入力冒頭を再利用する高速化機能。機密分離時は要検討 |
| ノイジーネイバー | 同居する重いリクエストが他の性能を圧迫する現象 |
| RAG | 社内文書を検索し、その内容を含めて回答を生成する方式 |
| 埋め込みモデル | 文章をベクトル化し、意味の近さを計算可能にするモデル |
| リランカー | 検索候補を精密に照合し直して並べ替えるモデル |
| 拡散モデル | ノイズから段階的に画像を生成する方式。LLMとは計算特性が異なる |
| LoRA | 少数の追加パラメータのみを学習する軽量なファインチューニング手法 |
| マルチLoRA | 複数のLoRAアダプタを同時にロードし、リクエスト単位で切り替える仕組み |
| vLLM | LLM配信用の推論エンジン。自社運用の事実上の標準 |
| NVLink | GPU同士を直結する高速インターコネクト |
| MIG | 1枚のGPUをハードウェア的に区切り、複数ワークロードで排他利用する機能 |

---

## 9. 出典と注意事項

本書に記載したモデル情報は2026年8月20日時点の公開情報に基づく。この領域は変化が速いため、**調達および導入の直前に必ず再確認すること**。特に以下は流動的である。

- Qwen3.8-Maxの重み公開スケジュール（2026年8月中旬予定）
- 各モデルのvLLM対応状況
- ライセンス条項（Kimi K3は独自ライセンスであり要法務確認）
- ベンチマーク順位（第三者検証の進捗により変動）

主な参照元:

- Hugging Face "State of Open Models: Summer 2026" — https://huggingface.co/blog/state-of-open-models-summer-2026
- Morph "The Best Open Source LLMs (2026)" — https://www.morphllm.com/best-open-source-llm
- Shakudo "Top 9 Large Language Models as of August 2026" — https://www.shakudo.io/blog/top-9-large-language-models
- vLLM 公式ドキュメント（分散推論・並列化） — https://docs.vllm.ai/
- GitLab Duo Self-Hosted 対応モデル一覧 — https://docs.gitlab.com/
