## 結論：用途別のおすすめ

| 用途 | 推奨モデル | サイズ(Q4相当) | 生成速度の目安 |
|---|---|---|---|
| 🗨️ 普段使いチャット | **gpt-oss-120b** | 約60GB | 約50 t/s |
| 💻 コード生成 | **Qwen3-Coder-Next (80B-A3B)** | 約46〜49GB | 高速（後述） |
| 🌐 翻訳 | **Qwen3.6-35B-A3B**（汎用）<br>or **plamo-2-translate**（専用） | 約22GB / 小型 | 約69 t/s |

---

## ⚠️ 大原則：Spark では「MoE 一択」

これが一番大事なポイントです。

DGX Spark は **128GB のメモリ容量**が魅力ですが、メモリ帯域は 273GB/s と控えめです。

> 📘 **メモリ帯域**とは、1秒あたりにメモリから読み出せるデータ量のこと。生成速度をほぼ直接決めます。RTX 5090 は約1.8TB/s なので、Spark はその1/6程度です。

そのため、モデルの「構造」で速度が激変します。

- **デンス（Dense）型** … 毎トークンで全パラメータを読む → 帯域で頭打ち
- **MoE 型**（Mixture of Experts）… 毎回一部の「専門家」だけを使う → 帯域を食わず速い

実測でも、ほぼ同サイズの Gemma 4 同士を比べると 26B-A4B（MoE）が 48.9 t/s、31B（デンス）が 10.6 t/s と約5倍の差が出ています。

❌ **避けたほうがいいもの**：Gemma 4 31B、Qwen3.6-27B などのデンス型（10 t/s 前後で「読む速度より遅い」）

---

## 用途別の詳細

### 🗨️ 普段使いチャット → gpt-oss-120b

128GB機の"主力"として最もバランスが良い選択です。gpt-oss-120b（o4-mini 級の賢さといわれる）が 50 t/s で動き、チャットの体感は「速い」とされています。

- 長文入力時も粘り強く、32Kトークンまで詰めても2〜3割減にとどまる
- 軽さ重視なら **gpt-oss-20b（12GB / 83 t/s）** をサブに置くのもアリ

### 💻 コード生成 → Qwen3-Coder-Next

80B総パラメータのうち**アクティブは3Bのみ**という構成で、Spark の帯域制約と非常に相性が良いモデルです。46GBのRAM（VRAM・ユニファイドメモリ可）があれば動作します。

- SWE-bench Pro で 44% を達成
- コンテキスト 256K → 大きめのリポジトリも扱える
- ⚠️ **量子化に注意**：3bit以下だと同じコードブロックを繰り返すループ現象のリスクが上がるため、最低4bit、可能なら Q6_K 以上が推奨されています。Spark はメモリに余裕があるので、Q6 を積極的に選んでください。

### 🌐 翻訳 → 2案あります

**案A：汎用モデルで済ませる（Qwen3.6-35B-A3B）**
- 22GB / 68.6 t/s と軽くて速い
- 日本語品質が高く、多言語もカバー
- 👍 チャット用と兼用できるので運用がラク

**案B：専用モデル（plamo-2-translate / PFN製）**
- プロンプト不要で、訳したい文章を丸投げするだけという運用のラクさが特徴
- 日英・英日に特化して精度が高い
- 🚨 **ライセンス要確認**：PLaMo Community License のもと、個人または年商10億円未満の企業であれば商用・非商用問わず無償でオンプレ利用可能という条件です。**大企業での業務利用は別途確認が必要**なので、ここは先に潰しておくのが安全です。

---

## 🖥️ 2台の役割分担案（クラスタを組まない前提）

実効メモリは 121GB 程度なので、大型モデル2本の同時常駐は KVキャッシュを考えると厳しいです。**1台1役**が素直です。

| | 常駐モデル | 役割 |
|---|---|---|
| **Spark A** | gpt-oss-120b（60GB） | チャット + 翻訳の汎用役 |
| **Spark B** | Qwen3-Coder-Next（Q6で60GB前後） | Cline / Continue などのコーディングバックエンド |

余ったメモリ枠に軽量な Qwen3.6-35B-A3B や plamo-2-translate を同居させると、翻訳だけ切り出す運用もできます。

---

## 💡 その他の実務メモ

- **推論エンジン**：まずは **llama.cpp（GGUF）**が一番実績が多く手軽です。複数ユーザーへの同時提供やスループット計測をしたい場合は vLLM に移行する流れが自然です。
- **消費電力**：計測中のピークで59〜90W。常時起動の推論サーバとして回しても電力面の心配はほぼありません。
- **ソフトの成熟が速い**：<cite name="7-1" index="7-1">発売直後（2025年10月）の公開値から、ドライバ・CUDA・llama.cpp の最適化だけで生成1.4倍・プリフィル2〜3倍に高速化</cite>しています。古いベンチ値を見て判断せず、必ず最新ビルドで測り直すのがおすすめです。

## 結論：「GPUメモリ」という固定枠は存在しません

Spark 1台あたりの数字は次のようになります。

| 段階 | 容量 | 備考 |
|---|---|---|
| 物理搭載 | **128GB** | LPDDR5X ユニファイドメモリ |
| OS が認識 | **約120GiB** | `free -h` の total 表示 |
| 実運用でモデルに使える目安 | **100〜110GB** | OS・ページキャッシュ・KVキャッシュ分を差し引いた現実値 |

クラスタを組まない場合、この 128GB プールが**1台ごとに独立**して存在します（2台合計256GBにはなりません）。

---

## 🔑 なぜ「VRAM ◯GB」と言えないのか

DGX Spark は **UMA（Unified Memory Architecture／統合メモリ）** を採用しています。

> 📘 **UMA** … CPU と GPU が**同じ物理メモリを共有**する方式。一般的なGPUのように「VRAM 24GB」という固定の枠が切られていません。

一般的なディスクリートGPUではVRAMとシステムRAMがPCIe越しの別プールですが、UMAではBlackwell GPU と ARM64 CPU の両方が単一の128GBプールにアクセスします。Spark には独立したVRAMが存在せず、CPUとGPUが1つのメモリプールを共有します。

つまり、**「GPUにいくら割り当てるか」は固定値ではなく、動的に決まる**というのが正しい理解です。

---

## ⚠️ nvidia-smi は使えません（重要）

Spark で `nvidia-smi` のメモリ使用量を見ると **"Not Supported"** と表示されます。これは故障ではなく仕様です。

NVIDIA の公式回答でも、統合メモリアーキテクチャのため NVIDIA-SMI は専用VRAMがある場合しかメモリ使用率を報告せず、`top` / `htop` / `free`、または DGX Dashboard（localhost:11000）を使うよう案内されています。

また、`cudaMemGetInfo` API でも実際より小さい空き容量が返るケースがあります。

### ✅ 正しい確認方法

```bash
free -h
```

見るべきは `free` 列ではなく **`available` 列**です。available はページキャッシュを考慮した「実際に新規ワークロードに使える量」を示します。

実例（前掲の記事より）：total が 120Gi、47Gi 使用中の状態で **available 72GiB** ＝ この72GiBが実際に使える枠、という読み方になります。

---

## 💡 モデルサイズを見積もるときの実務ポイント

- **🚨 swap を必ず 0 のまま維持する**
  推論中に swap が増え始めたら、重みがディスクにページアウトされている状態で、tok/s は一気に崩壊します。Spark の swap は他と同じNVMe上にあるため悪循環が深刻化します。
  → 対処は `--gpu-memory-utilization` を下げるか、小さいモデルに変えることです。

- **ページキャッシュとの取り合いに注意**
  UMA の副作用で、Linux のバッファキャッシュがメモリを食ってモデルロードを圧迫することがあります。

- **前回の構成案への当てはめ**
  gpt-oss-120b（約60GB）や Qwen3-Coder-Next（Q6で60GB前後）は **1台に1本なら余裕**、**2本同時常駐は KVキャッシュ込みで厳しい**、という線引きになります。1台1役の分担が現実的、という判断はこの数字から来ています。