# テナント別GPU基盤 構成設計書 第2版

作成日: 2026年8月26日
対象: 4テナント / 5チーム / AIユースケース50件
モデル情報の基準日: 2026年8月26日

---

## 1. 本書について

### 1.1 初版からの変更点

| 項目 | 初版 | 第2版 |
| --- | --- | --- |
| プールの名称 | P1、P2、Q1といった記号 | 役割を表す名称（高品質プール、補完プール等） |
| GLM-5.3の扱い | 移行候補 | 本命として位置づけ。GLM-5.2は重み公開までの暫定 |
| Kimi K3の扱い | 選定外 | 正式な構成案として追加（構成B） |
| 構成案の整理 | 標準構成と代替構成の2種 | 構成A・構成B・最小構成の3種と、選択プロセスを明示 |

初版でKimi K3を「選定しなかったモデル」として扱ったが、これは正確でない。Kimi K3はB300×8で動作する。常設構成に組み込む場合にサーバ全体の役割分担が変わるというだけである。本版ではこれを構成Bとして正式に記載する。

### 1.2 プール名称の対応表

初版から読み替える場合の対応は次のとおりである。

| 初版 | 第2版 |
| --- | --- |
| P1 | 高品質プール |
| P2 | 長文プール |
| P3 | 主力プール |
| P4 | 軽量プール |
| P5 | 補完プール |
| P6 | 検索・画像プール |
| Q1 | 予備プール |
| Q2 | バッチプール |
| Q3 | 学習・評価プール |
| Q4 | 分析プール |

> プールとは
> 特定の役割を割り当てたGPUのまとまりを指す。1つのプールが1つの推論エンドポイント（アプリケーションから見た接続先）に対応する。

---

## 2. 前提条件

### 2.1 組織構成

| 項目 | 内容 |
| --- | --- |
| チーム数 | 5 |
| テナント数 | 4（うち1テナントは2チームが共用） |
| ユースケース総数 | 50件 |
| 1チームあたり | 10件（均等分割） |

| テナント | 収容チーム数 | ユースケース数 |
| --- | --- | --- |
| テナントA | 1 | 10件 |
| テナントB | 1 | 10件 |
| テナントC | 1 | 10件 |
| テナントD | 2 | 20件 |

### 2.2 ハードウェア構成（各テナント）

| サーバ | 台数 | GPU総数 | GPUメモリ総量 |
| --- | --- | --- | --- |
| B300 × 8 | 1 | 8枚 | 2,304GB |
| H200 × 8 | 2 | 16枚 | 2,256GB |
| 合計 | 3 | 24枚 | 4,560GB |

GPUサーバの配置基準はテナント単位である。テナント間でのGPU共有および高品質モデルの集約は行わない。各テナントは自己完結した構成を取る。

> テナント分離とは
> 組織単位で計算資源とデータを隔離する方式である。本設計では、あるテナントのGPUサーバを別テナントから利用することはできない。したがって全機能を各テナント内で完結させる必要がある。

### 2.3 ユースケース構成

| カテゴリ | 全体 | 1チームあたり |
| --- | --- | --- |
| 情報収集 | 6件 | 約1件 |
| 生成・要約・集約 | 40件 | 8件 |
| データ分析（機械学習） | 2件 | 0〜1件 |
| RAG | 2件 | 0〜1件 |

コード生成支援・解析支援、GitLab Duo、画像生成は「生成・要約・集約」に含まれる。「生成・要約・集約」の内訳は、常時インタラクティブに操作するものとバッチ形式で動作可能なものが概ね半々である。

### 2.4 負荷の前提

1ユースケースあたり平均20〜30件の同時リクエストを想定する。ここでいうリクエストは人数ではなく、推論エンジンに到達する推論リクエストの単位である。

---

## 3. 需要の試算

### 3.1 総同時リクエスト数

| テナント | 単純計算 | 対話系 | バッチ系 |
| --- | --- | --- | --- |
| A / B / C（1チーム） | 200〜300 | 120〜180 | 80〜120 |
| D（2チーム） | 400〜600 | 240〜360 | 160〜240 |

バッチ系は夜間帯へ寄せることで日中の対話系ピークと衝突させない。したがって日中の設計目標は次のとおりとなる。

| テナント | 日中の対話系同時リクエスト（設計目標） |
| --- | --- |
| A / B / C | 120〜180 |
| D | 240〜360 |

### 3.2 必要スループットの概算

対話用途では1リクエストあたり毎秒20トークン程度の生成速度が実用上の下限となる。

```
テナントD（最大負荷）: 360 × 20 tok/s = 約7,200 tok/s
```

24枚構成で得られる集約スループットは概ね30,000〜50,000 tok/s と見込まれる。4倍から7倍の余裕がある計算になる。

> トークンとは
> モデルが文章を扱う際の最小単位である。日本語では概ね1文字から2文字が1トークンに相当する。

> スループットとレイテンシの違い
> スループットはサーバ全体が単位時間に処理できる総量、レイテンシは1件のリクエストが返るまでの時間である。両者はトレードオフの関係にあり、同時処理数を増やすとスループットは上がるがレイテンシは悪化する。

### 3.3 必要GPUメモリの内訳

```
必要GPUメモリ ＝ ①モデルの重み ＋ ②KVキャッシュ ＋ ③作業領域（10〜20%）
```

> KVキャッシュとは
> 大規模言語モデルが文章を生成する際、それまでに読んだ・書いたトークンの中間計算結果を保持しておく領域である。GPUメモリのうちモデルの重みを除いた残りが充てられる。同時リクエスト数とコンテキスト長の上限を直接決定する。

> コンテキスト長とは
> 1回のリクエストでモデルが同時に扱えるトークンの総量である。入力と出力の合計に適用される。128Kは約12万8千トークンを指す。128Kのリクエスト1件は8Kのリクエスト16件分のKVキャッシュを消費する。

実務上の指針として、重みは総メモリの50〜60%以下に抑える。これを超えるとKVキャッシュが枯渇し、同時リクエスト数かコンテキスト長のいずれかを大幅に削らざるを得なくなる。

---

## 4. モデル選定

### 4.1 採用モデル一覧

| 用途 | モデル | 規模（総／活性） | ライセンス | 選定理由 |
| --- | --- | --- | --- | --- |
| 高品質 | GLM-5.3 | 744B / 40B | 未表明（4.2項参照） | 品質と容量の両立。B300の4枚に収まる |
| 高品質（暫定） | GLM-5.2 | 744B / 40B | MIT | 5.3の重み公開までの代替。同一ベースのため差し替え容易 |
| 主力・長文 | DeepSeek V4 Flash | 284B / 13B | MIT | 活性パラメータが少なく高速。284B級の品質を13B相当の速度で得られる |
| 軽量 | Nemotron 3 Super | 120B / 12B | NVIDIA Open Model | 学習レシピまで公開され監査可能。1Mコンテキスト対応 |
| 補完 | Mistral Small 24B Instruct | 24B（密） | Apache 2.0 | GitLab Duoの3機能すべてに完全対応 |
| 補完 | Codestral 22B | 22B（密） | 独自 | GitLab Duoのコード補完・生成に完全対応 |
| 最大モデル（構成B） | Kimi K3 | 2.8T / 104B | 独自（要確認） | 現存する最大規模のオープンウェイトモデル |

> MoE（Mixture of Experts / 混合エキスパート）とは
> モデル内部を多数の専門家に分割し、入力に応じてごく一部だけを動作させる構造である。上表の「総／活性」のうち、総パラメータが必要GPUメモリを、活性パラメータが生成速度を決める。したがって「パラメータ数が大きい＝遅い」は成立しない。DeepSeek V4 Flashは総284Bだが活性13Bであり、メモリは284GB必要な一方、生成速度は13B相当となる。

> 密（Dense）モデルとは
> MoEと対比される従来型の構造で、全パラメータが常に動作する。総パラメータ数と活性パラメータ数が一致する。補完用途で用いる小型モデルはこちらである。

> FP8とは
> モデル内部の数値を8ビットで表現する方式である。1パラメータあたり1バイトを消費する。従来主流のBF16（2バイト）に対して必要メモリが半減する。この変換を量子化と呼ぶ。本書は全モデルをFP8前提で計算している。

### 4.2 GLM-5.3を本命とする理由と移行方針

GLM-5.3はGLM-5.2と同一のベースモデルに対し、事後学習のみを強化して構成されている。パラメータ規模（約743B）とコンテキスト長（1M）は同一である。したがってGPU配分を1枚も変更することなく差し替えが可能である。

性能面では、コーディングとエージェント処理において大幅な向上が報告されている。今回のユースケースはコード生成支援・解析支援を含むため、この向上は直接効いてくる。

現在の状況は次のとおりである。

| 項目 | 状況 |
| --- | --- |
| API提供 | 2026年8月14日に開始済み |
| 重みの公開 | 8月28日頃を予定。8月24日時点で公式リポジトリは未確認 |
| ライセンス | 未表明。GLM-5.1および5.2はMITであったが、5.3が同条件になる保証はない |

したがって運用方針は次のとおりとする。

1. 構築時点で重みが未公開であれば、GLM-5.2（MIT・公開済）で高品質プールを立ち上げる
2. GLM-5.3の重みが公開された時点でライセンスを確認する
3. ライセンスが許容可能であれば差し替える。GPU配分の変更は不要である

#### 移行時に注意すべき仕様変更

GLM-5.3では推論の努力度としてlow / high / maxの3段階が導入され、maxが既定値となった。加えて思考の無効化が一切できなくなっている。

この影響は次のとおりである。

| 影響 | 内容 |
| --- | --- |
| 出力トークン数の増加 | 思考部分もKVキャッシュを消費する |
| レイテンシの伸長 | 最初の応答が返るまでの時間が長くなる |
| 実効同時リクエスト数の低下 | 同じKV容量でも並列度は5.2より落ちる |

緩和策として、既定を低い努力度に設定し、レポート生成など品質を優先する用途にのみ高い努力度を指定する運用とする。

なお補完プールには引き続き24B級の密モデルを用いること。思考を無効化できないGLM-5.3は、応答速度が要件となるコード補完には適さない。

### 4.3 Kimi K3の位置づけ

Kimi K3はB300×8で動作する。メモリ面では余裕がある。

```
Kimi K3の重み: 約1,560GB（Hugging Faceでの実配布サイズ）
B300×8の容量: 2,304GB
KVキャッシュ残量: 約744GB（32Kコンテキストで概ね450並列相当）
```

制約は容量ではなく、8枚すべてを1つのモデルで使用する点にある。この場合サーバ1を2つのプールに分割できなくなるため、サーバ2およびサーバ3の役割分担を変更する必要がある。詳細は第6章の構成Bに記す。

生成速度については、活性パラメータがGLM-5.3の40Bに対して104Bであるため、単純なトークン生成速度は概ね2分の1から3分の1になる。ただしGLM-5.3は思考を無効化できず出力トークン数自体が増えるため、応答完了までの実時間で見た差はこれより小さくなる可能性がある。実測での確認が必要である。

### 4.4 常設構成に組み込まないモデル

以下は動作するが、本設計では常設構成に組み込まない。評価対象としては有効である。

| モデル | 規模 | 見送り理由 |
| --- | --- | --- |
| Qwen3.8-2.4T | 2.4T / 95B | FP8では2,304GBに収まらずFP4量子化が必須。活性95Bで生成が重い。独自ライセンス |
| DeepSeek V4-Pro | 1.6T / 49B | ライセンスはMITで明快だが、B300の8枚を専有する。Kimi K3と同じ制約を受けながら規模は下回るため、8枚を使うならKimi K3を優先する判断となる |
| MiniMax M3 | 428B / 23B | 独自ライセンスで商用制限があり、社内利用でも法務確認が必要 |
| Nemotron 3 Ultra | 550B / 55B | 高品質プールの代替候補。GLM系より品質は下回るがライセンスが明快。第10.5項の制約がある場合の代替として位置づける |

---

## 5. 構成A: 階層型（推奨）

品質・速度・冗長性・拡張性のすべてを確保する構成である。

### 5.1 全体像

```
テナント内（24枚 / 4,560GB）

  サーバ1: B300 × 8（2,304GB）
      ├─ 高品質プール （GPU 0-3）
      └─ 長文プール   （GPU 4-7）

  サーバ2: H200 × 8（1,128GB）
      ├─ 主力プール       （GPU 0-3）
      ├─ 軽量プール       （GPU 4-5）
      ├─ 補完プール       （GPU 6）
      └─ 検索・画像プール （GPU 7）

  サーバ3: H200 × 8（1,128GB）
      └─ 増強型 または 余裕型 を選択（5.4項）
```

### 5.2 サーバ1: B300 × 8

| プール | GPU | 並列構成 | 容量 | 搭載モデル | KV容量 | 想定同時 |
| --- | --- | --- | --- | --- | --- | --- |
| 高品質プール | 0-3 | TP=4 | 1,152GB | GLM-5.3（744B）／128K | 約408GB | 32K時 約250 ／ 128K時 約60 |
| 長文プール | 4-7 | TP=4 | 1,152GB | DeepSeek V4 Flash（284B）／128K | 約868GB | 32K時 約380 ／ 128K時 約95 |

> TP（テンソル並列 / Tensor Parallel）とは
> 1つのモデルを複数のGPUへ横方向に分割し、分担して処理させる方式である。TP=4は4枚のGPUで1つのモデルを動かすことを意味する。GPU間の通信量が非常に多いため、NVLink（GPU同士を直結する高速インターコネクト）で結ばれた同一サーバ内でのみ使用する。TPの値は実質的に1・2・4・8のいずれかとなる。モデルのアテンションヘッド数を割り切る必要があるため、3や5といった値は通常成立しない。

B300を高品質・長文の2プールに充てる理由は次の3点である。

1. GPUメモリが1枚あたり288GBあり、H200の141GBの2倍を超える。744B級モデルを4枚で運用でき、なおKVキャッシュを408GB確保できる
2. メモリ帯域が1枚あたり最大8TB/sであり、H200の4.8TB/sを上回る。長い出力を生成する用途で応答完了までの時間が短縮される
3. FP4をハードウェアで直接サポートするため、将来より大規模なモデルへ移行する際の選択肢が広い

高品質プールと長文プールで扱うモデルを分けているのは、両者の要求が異なるためである。高品質プールは品質を優先して744B級を、長文プールは同時処理数を優先して284B級を用いる。長文プールのKV容量が868GBと大きいのは、128Kのような長いコンテキストを多数並行して処理するためである。

### 5.3 サーバ2: H200 × 8

| プール | GPU | 並列構成 | 容量 | 搭載内容 | KV容量 |
| --- | --- | --- | --- | --- | --- |
| 主力プール | 0-3 | TP=4 | 564GB | DeepSeek V4 Flash（284B）／32K | 約280GB |
| 軽量プール | 4-5 | TP=2 | 282GB | Nemotron 3 Super（120B） | 約162GB |
| 補完プール | 6 | TP=1 × 3 | 141GB | Mistral Small 24B × 2 ／ Codestral 22B × 1 | 各約35GB |
| 検索・画像プール | 7 | — | 141GB | 埋め込みモデル、リランカー、拡散モデル | — |

> 埋め込みモデル（Embedding Model）とは
> 文章を数値のベクトルに変換するモデルである。文章同士の意味的な近さを距離として計算できるようになるため、RAGにおける関連文書の検索を担う。モデル自体は数GB程度と小さい。

> リランカー（Reranker）とは
> 埋め込みモデルが大まかに絞り込んだ候補文書を、質問文と1件ずつ精密に照合し直して並べ替えるモデルである。検索精度を大きく引き上げる。

> 拡散モデル（Diffusion Model）とは
> ノイズから段階的に画像を生成していく画像生成モデルの主流方式である。大規模言語モデルとは計算特性が大きく異なり、1枚の生成に数十秒を要する一方で使用メモリは少ない。言語モデルと同一GPUに同居させると相互に性能を阻害するため、検索・画像プールに隔離する。

補完プールを独立させる理由は2点ある。第一に、コード補完は1秒以内の応答が要件であり、長文処理に巻き込まれてはならない。第二に、GitLab Duo Self-Hostedは対応モデルの一覧が定められており、上位プールの大規模モデルを共用できないためである。

### 5.4 サーバ3: H200 × 8（構成を選択）

#### 増強型

サーバ2と同一の構成を取る。2台の前段に負荷分散を置き、処理能力を2倍にする。

| プール | GPU | 内容 |
| --- | --- | --- |
| 主力プール（2系統目） | 0-3 | サーバ2と同一 |
| 軽量プール（2系統目） | 4-5 | サーバ2と同一 |
| 補完プール（2系統目） | 6 | サーバ2と同一 |
| 検索・画像プール（2系統目） | 7 | サーバ2と同一 |

| 利点 | 欠点 |
| --- | --- |
| 処理能力が2倍になる | 高品質層の増強はできない |
| 片系停止時も全機能が継続する | バッチ・学習の専用枠がない |
| 無停止でのモデル更新が可能 | |

#### 余裕型

| プール | GPU | 容量 | 用途 |
| --- | --- | --- | --- |
| 予備プール | 0-3 | 564GB | DeepSeek V4 Flash（284B）。主力プールの2系統目 |
| バッチプール | 4-5 | 282GB | 夜間の一括生成・要約処理 |
| 学習・評価プール | 6 | 141GB | LoRA学習、新モデル評価 |
| 分析プール | 7 | 141GB | データ分析（回帰・分類）、周辺モデルの冗長 |

| 利点 | 欠点 |
| --- | --- |
| 主要機能に冗長性を確保しつつ、学習・評価の常設枠を持てる | 処理能力の増分は増強型より小さい |
| バッチ処理を日中の対話系から隔離できる | |

> LoRA（Low-Rank Adaptation）とは
> モデル全体ではなく、ごく一部の小さな追加パラメータのみを学習させる軽量な追加学習手法である。必要なGPUメモリが大幅に少なく済むため、限られた資源で用途特化を図る際の標準的な選択肢となる。

### 5.5 テナント別の推奨

| テナント | サーバ3 | 想定同時 | 判断根拠 |
| --- | --- | --- | --- |
| A / B / C | 余裕型 | 120〜180 | 処理能力に余裕があるため、学習・評価枠の確保を優先する |
| D | 増強型 | 240〜360 | 2チーム分の負荷を吸収するため、処理能力を優先する |

### 5.6 ルーティング設計

50件のユースケースが個別にモデル名とエンドポイントを直接記述する状態は運用不能となる。前段に単一の入口を設ける。

```
アプリケーション（用途名を指定）
        ↓
  ルーティング層（AI Gateway / LiteLLM 等）
        ↓
  ┌────────┬────────┬────────┬────────┐
 高品質   長文     主力     軽量     補完
```

要件は次のとおりである。

1. アプリケーション側は用途名のみを指定する（例: summarize-fast、report-generate）
2. 用途名からプールへのマッピングは基盤側で保持する
3. 全リクエストに用途識別子を必ず付与する。これがないと利用実態の把握もコスト按分も不可能になる
4. 優先度制御とレート制限をこの層で実施する

この構造により、モデルの入れ替え、A/B比較、利用量の可視化がすべて基盤側で完結する。GLM-5.2からGLM-5.3への差し替えも、この層の設定変更のみで完了する。

### 5.7 縮退時の動作

| 障害 | 挙動 | 影響 |
| --- | --- | --- |
| サーバ1（B300）停止 | サーバ2・3の主力プールで全用途を継続 | 品質が低下する。128Kの長文処理は不可 |
| サーバ2停止 | サーバ3が全機能を肩代わり | 処理能力が半減する |
| サーバ3停止 | サーバ2で継続 | 増強型では処理能力半減。余裕型では学習・評価が停止 |

いずれの単一障害でもサービスは継続する。ただしサーバ1停止時のみ高品質層と長文処理が失われるため、該当ユースケースの担当者へ事前に周知しておくこと。

---

## 6. 構成B: 最大モデル型

Kimi K3を常設する構成である。

### 6.1 構成

#### サーバ1: B300 × 8

| プール | GPU | 並列構成 | 容量 | 搭載モデル | KV容量 |
| --- | --- | --- | --- | --- | --- |
| 最大モデルプール | 0-7 | TP=8 | 2,304GB | Kimi K3（2.8T）／1M対応 | 約744GB |

#### サーバ2およびサーバ3: H200 × 8 × 2台

両サーバとも構成Aのサーバ2と同一構成とする（増強型が必須となる）。

| プール | GPU | 内容 |
| --- | --- | --- |
| 主力プール | 0-3 | DeepSeek V4 Flash（284B） |
| 軽量プール | 4-5 | Nemotron 3 Super（120B） |
| 補完プール | 6 | Mistral Small 24B × 2 ／ Codestral 22B × 1 |
| 検索・画像プール | 7 | 埋め込みモデル、リランカー、拡散モデル |

サーバ3を増強型に固定する理由は、構成Aで長文プールが担っていた処理能力をH200の2台で補う必要があるためである。結果として学習・評価プールとバッチプールは確保できなくなる。

### 6.2 構成Aとの比較

| 観点 | 構成A（階層型） | 構成B（最大モデル型） |
| --- | --- | --- |
| 最高品質モデル | GLM-5.3（744B / 40B） | Kimi K3（2.8T / 104B） |
| 高品質層の生成速度 | 基準 | 概ね2分の1から3分の1 |
| 高品質層のコンテキスト | 128K | 1M |
| 主力層の総容量 | B300の長文プール ＋ H200 2台 | H200 2台のみ |
| 学習・評価の常設枠 | 余裕型で確保可能 | なし |
| バッチ専用枠 | 余裕型で確保可能 | なし（夜間の時間帯分離で対応） |
| ライセンス | GLM-5.3は未表明 | 独自ライセンス。要法務確認 |
| モデル配布量（4テナント計） | 約4.9TB | 約8.2TB |
| 縮退時（サーバ1停止） | 主力プールで継続 | 主力プールで継続 |

### 6.3 構成Bを採用する条件

以下をすべて満たす場合に限り、構成Bが妥当である。

1. 10件のユースケースのうち2〜3件が、2.8T級の品質でなければ成立しない
2. その用途は応答時間を待てる（数十秒から数分を許容できる）
3. 学習・評価の常設枠を手放せる。または評価専用環境を別途確保できる
4. 4テナント合計で約8.2TBのモデル配布運用に耐えられる
5. Kimi K3の独自ライセンスについて法務確認が完了している

1つでも満たさない場合は構成Aを採用し、Kimi K3は第9章の段階的評価プロセスで扱うこと。

---

## 7. ユースケースのマッピング

### 7.1 カテゴリ別の割り当て（構成A）

| カテゴリ | 件数（1チーム） | 割当プール | 応答要件 | 備考 |
| --- | --- | --- | --- | --- |
| 情報収集 | 約1件 | 軽量プール／夜間はバッチプール | 準リアルタイム | 索引化は検索・画像プール。バッチ化しやすく最優先で夜間へ回す |
| 生成・要約・集約（対話系） | 約4件 | 長文プール・主力プール | 準リアルタイム | 全体の主役。ここの容量が最も効く |
| 生成・要約・集約（バッチ系） | 約4件 | 夜間の長文プール・バッチプール | バッチ | 日中の対話系と時間帯を分離する |
| うちコード補完・GitLab Duo | 上記に含む | 補完プール | 即時 | 必ず専用プールへ分離する |
| うちコード解析・レビュー | 上記に含む | 高品質プール | 準リアルタイム | 長文入力のため128K設定を使う |
| うちレポート生成 | 上記に含む | 高品質プール | 準リアルタイム | 品質が成果物の価値を直接左右する |
| うち画像生成 | 上記に含む | 検索・画像プール | 非同期 | 生成に数十秒を要するため非同期処理を前提とする |
| データ分析（機械学習） | 0〜1件 | 分析プール | バッチ | 第10.1項の解釈確認が前提 |
| RAG | 0〜1件 | 検索: 検索・画像プール ／ 生成: 主力プール（高精度時は高品質プール） | 準リアルタイム | 5チームで2件のみのため該当チームのみ構成 |

構成Bの場合、上表の「高品質プール」と「長文プール」への割り当てをすべて「最大モデルプール」に読み替える。

### 7.2 エンドポイント数への集約

50件のユースケースは、最終的にテナントあたり6本のエンドポイントへ集約される。

| エンドポイント | 想定件数（1チーム） | 内容 |
| --- | --- | --- |
| 高品質プール | 2〜3件 | 品質と長文が要件を左右する用途 |
| 長文プール・主力プール | 5〜6件 | 日常業務の大半 |
| 軽量プール | 1件 | 軽量な整形・分類 |
| 補完プール | 1件（ただし高頻度） | コード補完・GitLab Duo |
| 検索・画像プール | 0〜1件 | RAG検索、画像生成 |
| 分析プール | 0〜1件 | 回帰・分類 |

### 7.3 1つのモデルを複数用途で共有する仕組み

大規模言語モデルの推論はステートレスである。1リクエストごとに入力が完結して届き、処理後に何も残らないため、異なる用途のリクエストが混在しても互いに干渉しない。

> ステートレスとは
> 前後のリクエスト間で状態を引き継がない性質を指す。会話履歴はサーバ側に保持されるのではなく、毎回リクエストに含めて送信される。

用途ごとの差異は、GPUを分けることなく上位層で吸収する。

| 層 | 手段 | 例 |
| --- | --- | --- |
| アプリケーション層 | システムプロンプト | 役割の指定 |
| | 生成パラメータ | レポート生成は temperature 0.7 / max_tokens 8000、分類は temperature 0 / max_tokens 50 |
| | 出力形式の指定 | JSONスキーマを渡して構造化出力を強制する |
| | 入力コンテキスト | RAG検索結果、ソースコード、社内文書 |
| ルーティング層 | 優先度付与、レート制限、監査ログ | 用途識別子による分類 |
| 推論層 | 変更なし | モデルは共通 |

推論エンジンは継続バッチングにより、用途の異なるリクエストを自動的に同一バッチへまとめて処理する。

> 継続バッチング（Continuous Batching）とは
> 従来の推論エンジンは一定件数が溜まるまで待ってから一括処理していたが、現行のエンジンは処理が完了したリクエストの枠に次のリクエストを即座に差し込む。用途が混在してもGPU利用効率が低下しない。

---

## 8. 最小構成（H200 × 8 を1台のみとする場合）

B300が調達できず、H200×8を1台のみとする場合の構成である。総容量は1,128GBとなり、構成Aの約4分の1である。

### 8.1 構成

| プール | GPU | 並列構成 | 容量 | 搭載内容 | KV容量 |
| --- | --- | --- | --- | --- | --- |
| 主力プール | 0-3 | TP=4 | 564GB | DeepSeek V4 Flash（284B）／32K | 約280GB |
| 軽量プール | 4-5 | TP=2 | 282GB | Nemotron 3 Super（120B） | 約162GB |
| 補完プール | 6 | TP=1 × 3 | 141GB | Mistral Small 24B × 2 ／ Codestral 22B × 1 | 各約35GB |
| 検索・画像プール | 7 | — | 141GB | 埋め込みモデル、リランカー、拡散モデル | — |

### 8.2 構成Aとの差分

| 項目 | 構成A | 最小構成 |
| --- | --- | --- |
| 最上位モデル | GLM-5.3（744B） | DeepSeek V4 Flash（284B） |
| 128Kコンテキスト | 対応 | 非対応（32Kが上限） |
| 冗長性 | あり | なし |
| 無停止でのモデル更新 | 可能 | 不可（停止窓が必要） |
| 学習・評価の常設枠 | あり | なし |
| バッチ専用枠 | あり | なし（対話系と同一プールで時間帯分離） |

### 8.3 成立条件と制約

| テナント | 判定 | 内容 |
| --- | --- | --- |
| A / B / C（同時120〜180） | 成立する | 余裕はないが要件は満たせる |
| D（同時240〜360） | 厳しい | 下記の対策が必須となる |

テナントDで最小構成を採る場合、次の対策を必須とする。

1. バッチ化の徹底。生成・要約・集約のうちバッチ可能な用途は例外なく夜間へ移す
2. ルーティング層でのレート制限。ユースケース単位で同時リクエスト数の上限を設定する
3. コンテキスト長の抑制。既定を16Kとし、長文が必要な用途のみ個別に許可する
4. 優先度制御の厳格な運用。コード補完を最優先とし、レポート生成等を下位に置く

### 8.4 運用上の留意事項

冗長性がないため、モデル更新およびドライバ更新時にはサービス停止が発生する。計画停止の窓を事前に確保し、利用部門と合意しておくこと。

また、最小構成では744B級モデルを載せられない。1,128GBに744GBの重みを置くとKVキャッシュが384GBとなり、他のプールを配置する余地がなくなるためである。高品質層が必要な用途がある場合、構成Aとの品質差を事前に評価しておくことが望ましい。

---

## 9. 構成の選び方と段階的評価プロセス

### 9.1 判断フロー

```
  B300×8 が確保できるか
      ├─ できない ─→ 最小構成（第8章）
      └─ できる
           ↓
  2.8T級の品質でなければ成立しない用途があるか
      ├─ ない ─→ 構成A（第5章）
      └─ ある
           ↓
  第6.3項の5条件をすべて満たすか
      ├─ 満たさない ─→ 構成A ＋ 段階的評価（9.2項）
      └─ 満たす ─→ 構成B（第6章）
```

### 9.2 段階的評価プロセス

最初から構成Bを常設とすることは推奨しない。品質差が業務価値に見合うかは、実際の業務データで測定しないと判断できないためである。ベンチマークの公表順位では判断できない。

推奨する手順は次のとおりである。

| 段階 | 内容 |
| --- | --- |
| 第1段階 | 構成Aで立ち上げる。GLM-5.2を高品質プールに配置し、GLM-5.3の重み公開後に差し替える |
| 第2段階 | サーバ3を一時的に増強型へ変更し、本番配信を肩代わりさせる |
| 第3段階 | サーバ1をKimi K3のTP=8構成へ切り替え、期間を区切って評価する |
| 第4段階 | 高品質プールを使う2〜3件のユースケースについて、実業務データで品質差を測定する |
| 第5段階 | 差が価値に見合えば構成Bへ移行する。見合わなければ構成Aへ戻す |

評価にあたっては、次の観点を数値で記録すること。

- 出力品質（業務担当者による評価。可能であればブラインド比較）
- 応答完了までの実時間（トークン生成速度ではなく、利用者から見た待ち時間）
- 同一負荷での同時処理数
- 学習・評価枠を失うことによる運用上の影響

---

## 10. 設計上の論点

### 10.1 「データ分析（機械学習）」の解釈確定

2通りの解釈が可能であり、必要リソースが一桁変わる。

| 解釈 | 必要なもの |
| --- | --- |
| A. 古典的な機械学習（XGBoost、scikit-learn等） | GPUはほぼ不要。CPUサーバで十分 |
| B. 言語モデルに分析コードを書かせる、結果を解釈させる | 主力プールで対応可能 |
| C. 深層学習モデルの学習（時系列・表形式ニューラルネット等） | 分析プールを専有 |

該当が2件のみであるため影響は限定的だが、Aであれば本GPU基盤を割り当てるべきではない。早期の切り分けを推奨する。

### 10.2 ノイジーネイバー問題

同一エンドポイントを共有する以上、長大なリクエストが他を圧迫する。

> ノイジーネイバーとは
> 共有環境において、一部の利用者が過大な資源を消費し、他の利用者の性能を劣化させる現象を指す。

| 状況 | 影響 |
| --- | --- |
| 128Kコンテキストのレポート生成が1件走る | 8Kのリクエスト16件分のKVキャッシュを占有する |
| 出力8,000トークンの生成が長時間居座る | バッチ枠を塞ぎ、後続の待ち時間が伸びる |

ソフトウェア的な優先度制御は、GPU演算資源の奪い合いに対しては十分に機能しない。確実性を求めるならプールごとの物理分離が唯一の解である。本設計で補完プールを独立させているのはこのためである。

### 10.3 テナント分離に伴う運用設計

テナント間で資源を共有できないことにより、以下の運用負荷が4倍になる。

#### モデル配布

閉域環境では、これが最も重い運用コストとなる。テナント境界を越えて転送する必要のあるモデルの総量は次のとおりである。

| 構成 | 1テナント | 4テナント合計 |
| --- | --- | --- |
| 構成A | 約1.22TB | 約4.9TB |
| 構成B | 約2.04TB | 約8.2TB |
| 最小構成 | 約0.48TB | 約1.9TB |

構成Aの内訳は、GLM-5.3が744GB、DeepSeek V4 Flashが284GB、Nemotron 3 Superが120GB、補完用の小型モデル2種が計約46GB、周辺モデルが約30GBである。構成BはGLM-5.3をKimi K3（約1,560GB）に置き換えた場合の値である。

モデルを1回更新するたびに、この量を4テナントへ配布することになる。テナント境界を跨げる読み取り専用のモデルレジストリを設計に含めること。これがない場合、更新のたびに人手で媒体を運搬する運用となる。

#### バージョン統制

4テナントでモデルバージョンがずれると、チーム間での評価結果の比較、共通ツールの動作保証、障害時の切り分けがいずれも成立しなくなる。特にGLM-5.2から5.3への差し替えは、全テナントで同時期に実施すること。

中央で承認済みモデルカタログを管理し、各テナントはそこからのみ取得する運用とする。

#### 評価・検証の重複

新モデルの評価を各テナントで個別に実施するのは非効率である。本番4テナントとは別に、小規模でよいので評価専用の環境を設けることを推奨する。既存のDGX Sparkのような小型機でも、モデルの動作確認とプロンプト検証の範囲であれば成立する。

#### 周辺モデルの重複保持

埋め込みモデル、リランカー、拡散モデルはテナントごとに配置する必要がある。ただしこれらは合計でも数十GB程度であり、容量面の影響は軽微である。バージョン管理の手間のみが4倍となる。

### 10.4 モデル情報の鮮度

本書のモデル情報は2026年8月26日時点の公開情報に基づく。この領域は変化が速いため、調達および構築の直前に再確認すること。特に以下は流動的である。

- GLM-5.3の重み公開時期とライセンス（第4.2項）
- Kimi K3のライセンス条項（独自ライセンスであり要法務確認）
- 各モデルの推論エンジン対応状況。新しいアーキテクチャは対応まで数週間から数ヶ月を要する場合がある。導入予定モデルは対応状況を先に確認すること
- ベンチマーク順位。第三者による独立検証が追いついていないモデルが多い。自社の評価セットで測定するまでは公開されている順位表を判断材料にしない

### 10.5 ベンダーの偏在

2026年以降、最大規模かつ最高性能のオープンウェイトモデルは中国系の研究機関から継続的に公開されている。米国系の公開モデルは、NVIDIAのNemotron系等の例外を除き規模が一段小さい。

重みが公開され閉域環境で動作する以上、技術的リスクは限定的である。ただし組織の調達規程やセキュリティ規程が開発元に制約を設けている場合があるため、事前確認を推奨する。

制約がある場合の代替は次のとおりとなるが、いずれも規模は一段下がる。

| プール | 代替候補 |
| --- | --- |
| 高品質プール | Nemotron 3 Ultra（550B / 55B、NVIDIA Open Model） |
| 長文プール・主力プール | Nemotron 3 Super（120B / 12B、NVIDIA Open Model） |
| 補完プール | Mistral Small 24B Instruct（Apache 2.0）、gpt-oss（OpenAI） |

---

## 11. 導入ステップ

### 第1段階: 前提の確定

1. 「データ分析」の解釈を確定する（第10.1項）
2. GitLab Duoの必要機能と対応モデルを公式一覧で確定する
3. 50件のうち128Kコンテキストを要するものを洗い出す
4. RAGとデータ分析の担当チームを特定し、検索・画像プールと分析プールの構成を確定する
5. 開発元に関する組織規程の制約有無を確認する（第10.5項）
6. 2.8T級の品質が必須となる用途の有無を確認し、構成A・構成Bを仮決めする（第9.1項）

### 第2段階: 検証

7. 50件を高品質・主力・補完の3クラスへ仕分ける
8. 各クラスの代表ユースケースを1件ずつ選び、評価環境で実測する
9. 同時リクエスト数を振ってTTFTとTPOTのカーブを取得する

> TTFT（Time To First Token）とは
> リクエスト送信から最初の1文字が返るまでの時間である。体感速度を左右し、対話用途では1〜2秒以内が目安となる。

> TPOT（Time Per Output Token）とは
> 1トークンを生成するのに要する時間である。文章が流れる速さに相当し、人間の読む速度である毎秒10〜20トークンが実用上の下限となる。

いずれも平均値ではなくp95（95%のリクエストが満たすべき値）で目標設定する。

### 第3段階: 構築

10. ルーティング層を先に設置し、アプリケーションからの推論エンジンへの直接接続を許可しない
11. モデルレジストリと配布経路を整備する（第10.3項）
12. 1テナントで構成Aを構築し、動作を確認する
13. 残る3テナントへ同一構成を展開する

### 第4段階: 運用開始と調整

14. まず配分の70%で起動し、実利用データを見て残り30%を調整する
15. 用途識別子ごとの利用量を可視化し、プール配分の妥当性を検証する
16. サーバ3の構成（増強型・余裕型）をテナントごとに見直す
17. GLM-5.3の重みが公開されたらライセンスを確認し、全テナントで同時期に差し替える

### 第5段階: 最大モデルの評価（該当する場合のみ）

18. 第9.2項の段階的評価プロセスに従い、Kimi K3の導入可否を判断する

机上計算のみで確定させないこと。特にMoEモデルは重みと実際の計算量の乖離が大きく、実測との差が出やすい。

---

## 12. 用語集

| 用語 | 説明 |
| --- | --- |
| プール | 特定の役割を割り当てたGPUのまとまり。1プールが1推論エンドポイントに対応する |
| テナント分離 | 組織単位で計算資源とデータを隔離する方式。本設計ではテナント間のGPU共有を行わない |
| KVキャッシュ | 生成途中の中間結果を保持する領域。同時リクエスト数とコンテキスト長の上限を決める |
| コンテキスト長 | 1リクエストで扱えるトークン総量。入力と出力の合計に適用される |
| トークン | モデルが文章を扱う最小単位。日本語では概ね1〜2文字が1トークンに相当する |
| MoE（混合エキスパート） | 内部を専門家に分割し一部のみ動作させる構造。総パラメータがメモリを、活性パラメータが速度を決める |
| 密（Dense）モデル | 全パラメータが常に動作する従来型構造。総パラメータと活性パラメータが一致する |
| FP8 / BF16 / FP4 | 数値の表現精度。1パラメータあたり順に1バイト・2バイト・0.5バイトを消費する |
| 量子化 | 数値の表現精度を下げてメモリ消費を減らす手法。わずかな精度低下と引き換えとなる |
| TP（テンソル並列） | 1モデルを複数GPUへ横分割する方式。通信量が多く同一サーバ内に限定される |
| PP（パイプライン並列） | 1モデルを層単位で縦分割する方式。通信量が少なくサーバ間向き |
| Prefill | 入力を読み込む段階。演算性能が律速となる |
| Decode | 1トークンずつ生成する段階。メモリ帯域が律速となる |
| TTFT | 最初の1文字が返るまでの時間。体感速度の指標 |
| TPOT | 1トークンあたりの生成時間。文章が流れる速さの指標 |
| p95 | 95%のリクエストが満たす値。性能目標は平均ではなくこの値で設定する |
| 継続バッチング | 完了したリクエストの枠へ次を即座に差し込む方式。用途が混在しても効率が落ちない |
| ステートレス | 前後のリクエスト間で状態を引き継がない性質 |
| ノイジーネイバー | 共有環境で一部の利用者が資源を占有し、他の性能を劣化させる現象 |
| 推論の努力度 | GLM-5.3で導入された設定。low / high / max の3段階があり、思考の量を制御する |
| RAG | 社内文書を検索し、その内容を含めて回答を生成する方式 |
| 埋め込みモデル | 文章をベクトル化し、意味の近さを計算可能にするモデル |
| リランカー | 検索候補を精密に照合し直して並べ替えるモデル |
| 拡散モデル | ノイズから段階的に画像を生成する方式。言語モデルとは計算特性が異なる |
| LoRA | 少数の追加パラメータのみを学習する軽量な追加学習手法 |
| NVLink | GPU同士を直結する高速インターコネクト |
| モデルレジストリ | 承認済みモデルを一元管理し、各環境へ配布する仕組み |

---

## 13. 出典

本書のモデル情報は以下を参照している。数値および状況は変動するため、利用時点での再確認を前提とすること。

- Hugging Face「State of Open Models: Summer 2026」 https://huggingface.co/blog/state-of-open-models-summer-2026
- Z.ai GLM-5.3 リリース情報 https://datanorth.ai/news/z-ai-releases-glm-5-3
- GLM-5.3 オープンウェイト公開状況とライセンス https://www.modemguides.com/blogs/ai-news/glm-5-3-open-weights-security-findings
- GLM-5.3 仕様および公開状況の追跡 https://evolink.ai/blog/glm-5-3-release
- vLLM 公式ドキュメント（分散推論・並列化） https://docs.vllm.ai/
- GitLab Duo Self-Hosted 対応モデル一覧 https://docs.gitlab.com/
