# GPU基盤 ユースケース配分設計案

対象構成: NVIDIA B300 × 8（1台） / NVIDIA H200 × 4（1台）
対象ユースケース: 8分野・約50件

---

## 1. 前提となる設計方針

### 1.1 「50ユースケース ≠ 50モデル」

本設計における最重要の前提である。

> **50件のユースケースを、6〜8本の共有エンドポイントに集約する。**

ユースケースごとに個別のモデルを起動すると、GPUメモリはモデルの重み（パラメータそのもの）だけで埋まり、**KVキャッシュ**を確保する余地がなくなる。結果として「モデルは載っているが同時に数人しか使えない」状態に陥る。

> **KVキャッシュとは**
> 大規模言語モデル（LLM）が文章を生成する際、これまで読んだ／書いたトークンの中間計算結果を保持しておく領域である。会話が長いほど、同時利用者が多いほど大きくなり、**同時接続数の上限を直接決定する**。GPUメモリの配分において、モデルの重みと並ぶ二大要素である。

したがって、分割の単位は「用途の数」ではなく「**性能クラスの数**」とする。

### 1.2 ユースケースの分類軸

50件を以下の2軸で仕分ける。

| 軸 | クラス | 説明 |
| --- | --- | --- |
| レイテンシ要件 | P0: 即時（〜1秒） | 入力に追従する必要があるもの。コード補完など |
| | P1: 準リアルタイム（〜10秒） | 人が待てる範囲。対話・要約など |
| | P2: バッチ（分〜時間） | 即時性不要。夜間実行可能なもの |
| 品質要件 | 高 | フロンティア級モデル（最大級のモデル）が必須 |
| | 中 | 70B級で十分 |
| | 小 | 24B級で十分 |

> **「B」とは**
> パラメータ数の単位で、B = Billion（10億）を表す。70Bなら700億パラメータのモデルを指す。数が大きいほど高品質だが、必要なGPUメモリと計算量も増える。

---

## 2. B300 × 8 の配分案（総容量 2,304GB）

LLMによる本番配信を全てこのサーバに集約する。

| プール | 割当GPU | 容量 | 搭載モデル | 想定同時実行数 |
| --- | --- | --- | --- | --- |
| P1: 高品質 | 0-3（TP=4） | 1,152GB | 235B級 MoE（FP8）<br>重み235GB → KV 800GB | 500〜600 |
| P2: 主力量産 | 4-5（TP=2） | 576GB | 70B級（FP8）<br>重み70GB → KV 450GB | 300〜400 |
| P3: 低遅延・小型 | 6 | 288GB | 24B級 × 3〜4レプリカ | 200〜400 |
| P4: 予備／バッチ | 7 | 288GB | 可変 | — |

**合計の即時対応力: 同時1,000〜1,300リクエスト**

> **TP（テンソル並列 / Tensor Parallel）とは**
> 1つのモデルを複数のGPUに横方向に分割し、分担して処理させる方式である。「TP=4」は4枚のGPUで1つのモデルを動かすことを意味する。GPU間の通信量が非常に多いため、NVLink（GPU同士を直結する高速インターコネクト）で結ばれた同一サーバ内でのみ使用する。

> **MoE（Mixture of Experts / 混合エキスパート）とは**
> モデル内部を複数の「専門家（エキスパート）」に分割し、入力に応じて一部だけを動作させる構造である。総パラメータ数の割に計算量が少なく、KVキャッシュも軽い傾向にあるため、**パラメータ数が大きくても同時接続数を稼ぎやすい**という特性を持つ。「パラメータ数が大きい＝重い」は成立しない点に注意が必要である。

> **FP8 / BF16 とは**
> モデルの数値を何ビットで表現するかを示す精度の指定である。BF16は1パラメータあたり2バイト、FP8は1バイトを消費する。FP8にすると必要メモリが半減する代わりに、わずかな精度低下が生じうる。これを一般に**量子化**と呼ぶ。

### P4（予備／バッチ枠）の運用

時間帯によって役割を切り替えることで稼働率を高める。

- 日中: P1/P2の急激な負荷増（バースト）の吸収、および障害発生時の退避先
- 夜間: バッチ処理、LoRAによる追加学習、モデル評価の実行

> **LoRA（Low-Rank Adaptation）とは**
> モデル全体ではなく、ごく一部の小さな追加パラメータのみを学習させる軽量なファインチューニング手法である。必要なGPUメモリが大幅に少なく済むため、限られた資源で追加学習を行う際の標準的な選択肢となる。

---

## 3. H200 × 4 の配分案（総容量 564GB）

「LLMによる対話以外」のワークロードを集約する。性質の異なる処理をB300から隔離することが目的である。

| プール | 割当GPU | 用途 | 対応カテゴリ |
| --- | --- | --- | --- |
| R1: 検索基盤 | 0 | 埋め込みモデル＋リランカー<br>（小容量・高頻度） | 内部RAG／外部情報収集 |
| R2: 画像生成 | 1 | 拡散モデル（FLUX/SDXL系）<br>負荷特性が異なるため専有 | まとめ生成＋画像作成 |
| R3: データ分析 | 2 | 機械学習モデルの学習・推論 | 回帰／分類 |
| R4: 検証・評価 | 3 | モデル差し替え自由枠<br>B300縮退時のバックアップ | 全カテゴリ横断 |

> **埋め込みモデル（Embedding Model）とは**
> 文章を数値のベクトルに変換するモデルである。文章同士の意味的な近さを距離として計算できるようになるため、RAGにおける「関連文書の検索」の中核を担う。

> **リランカー（Reranker）とは**
> 埋め込みモデルが大まかに絞り込んだ候補文書を、質問文と1件ずつ精密に照合し直して並べ替えるモデルである。検索精度を大きく引き上げる役割を持つ。

> **RAG（Retrieval-Augmented Generation / 検索拡張生成）とは**
> 社内文書などを検索し、その内容をLLMへの入力に含めた上で回答を生成する方式である。モデル自体を再学習させることなく、社内固有の知識に基づいた回答を得られる。

> **拡散モデル（Diffusion Model）とは**
> ノイズから段階的に画像を生成していく画像生成モデルの主流方式である。LLMとは計算特性が大きく異なり、1枚の生成に数十秒を要する一方で使用メモリは少ない。**LLMと同一GPUに同居させると相互に性能を阻害する**ため、専有させることが望ましい。

---

## 4. 8カテゴリとプールの対応表

| # | カテゴリ | 主プール | クラス | 補足 |
| --- | --- | --- | --- | --- |
| 1 | レポート生成 | P1 | P1・高 | 長文出力。品質重視。件数が少ないため同時実行数は消費しない |
| 2 | 外部情報収集 | P2（夜間はP4） | P2・中 | 索引化はR1。バッチ化しやすく、最優先で夜間へ回すべき対象 |
| 3 | 情報集約／要約 | P2 | P1/P2・中 | 入力が長くPrefill重め。定期実行分はP4へ |
| 4 | データ分析 | R3 ＋ P2 | P2・中 | 5.2項の注意点を参照 |
| 5 | コード生成支援 | 補完 → P3<br>解析・レビュー → P1 | P0・小/高 | 性質が正反対のため必ず分離する |
| 6 | 内部ドキュメントRAG | 検索 → R1<br>生成 → P2（高精度時P1） | P0/P1・中 | 最も利用者数が伸びやすい。要監視 |
| 7 | まとめ生成＋画像作成 | 文章 → P2<br>画像 → R2 | P1・中 | 画像生成に数十秒を要するため非同期処理を前提とする |
| 8 | GitLab Duo | 補完 → P3<br>Chat → P2 | P0・小/中 | 5.1項の注意点を参照 |

> **Prefill と Decode とは**
> LLMの推論は2つの段階に分かれる。**Prefill**は入力プロンプト全体を読み込む段階で、計算性能（演算器）が律速となる。**Decode**は1トークンずつ生成していく段階で、メモリ帯域が律速となる。RAGや長文要約は入力が長いためPrefill重め、対話やエージェント処理は出力が長いためDecode重めとなり、効いてくるハードウェア性能が異なる。

---

## 5. 設計上ハマりやすい5つの論点

### 5.1 GitLab Duo はモデルを自由に選択できない

GitLab Duo Self-Hostedには公式の対応モデル一覧が定められており、機能ごとに「完全対応／部分対応／非対応」が明確に区分されている。例として、Codestral 22Bはコード補完・コード生成に完全対応する一方でChatは対象外、Mistral Small 24B Instructは3機能すべてに完全対応、Llama 3.1 70Bはコード生成に完全対応、といった具合である。

配信基盤としてvLLMは公式サポート対象であり、エンドポイントURLは末尾に `/v1` を付与して指定する。

**すなわち、Duo は P1 の高品質モデルを共用できない。** P3にDuo専用エンドポイントを立てる前提で設計する必要がある。着手順としては、公式の対応表で必要機能（補完／生成／Chat）を満たすモデルを確定させることが先行する。

> **vLLM とは**
> LLMを高速かつメモリ効率よく配信するための推論エンジンである。KVキャッシュを細かいブロック単位で管理する仕組み（PagedAttention）により、同時接続数を大きく伸ばせる点が特徴で、自社運用LLM基盤の事実上の標準となっている。

### 5.2 「データ分析（回帰／分類）」の解釈確定が必要

2通りの解釈が可能であり、必要リソースが一桁変わる。

| 解釈 | 必要なもの |
| --- | --- |
| A. 古典的な機械学習（XGBoost、scikit-learn等） | GPUはほぼ不要。CPUサーバで十分 |
| B. LLMに分析コードを書かせる／結果を解釈させる | P2で対応可能 |
| C. 深層学習モデルの学習（時系列・表形式ニューラルネット等） | R3を専有 |

**Aが大半であれば、本GPU基盤を割り当てるべきではない。** 早期に切り分けることを推奨する。

### 5.3 優先度の逆転を物理分離で防ぐ

コード補完（P0・1秒以内が要件）が、夜間バッチや長文レポート生成に巻き込まれて遅延する——これが最も発生しやすい事故である。

ソフトウェア的な優先度制御は、GPUの計算資源の奪い合いに対しては十分に機能しない。**P0系はGPUプールごと物理的に分離する**ことが確実である。第2章の配分案でP3を独立させているのはこのためである。

### 5.4 ルーティング層を必ず1枚挟む

50個のアプリケーションがそれぞれモデル名とエンドポイントを直接記述している状態は、運用不能に陥る。

- 単一の入口（AI Gateway / LiteLLM / vLLM router 等）を設置する
- アプリケーション側は「用途名」のみを指定する（例: `summarize-fast`）
- 用途名からプールへのマッピングは基盤側で保持する

この構造により、モデルの入れ替え、A/B比較、利用量の可視化がすべて基盤側で完結する。

### 5.5 セキュリティドメイン分離が最大の変数

機密区分に応じて処理面を物理分離する必要があるか否かで、本設計案は根本から変わる。分離が必要な場合、区分ごとに「モデルが載る最小構成」が求められ、B300の8枚を4+4に割るような設計となる。

**配分を確定する前に、この点を先に固めることが必須である。**

---

## 6. キャパシティの見立て

**結論として、50件のポートフォリオに対して演算能力には十分な余裕がある。**

- 社内利用で数千〜1万人規模であれば、本配分案で吸収可能である
- ボトルネックとなるのは性能ではなく**運用設計**（モデル管理、優先度制御、利用量の可視化）である

ただし、以下に該当するユースケースが含まれる場合は個別の試算が必要となる。

- 全社員が常時利用するコード補完（同時実行数が跳ね上がる）
- 大量の外部公開向け処理
- リアルタイム性を要する画像の大量生成

---

## 7. 進め方の提案

1. **50件を2軸（レイテンシ × 品質）で仕分ける** — 経験上、6〜7クラスに収束する
2. **クラスごとに代表ユースケースを1件選び、H200のR4で実測する**
3. **同時実行数を振ってTTFT/TPOTのカーブを取得し、それからB300の配分を確定する**
4. **まず本案の70%配分で起動し、実利用データを見て残り30%を調整する**

> **TTFT / TPOT とは**
> **TTFT**（Time To First Token）は、リクエスト送信から最初の1文字が返るまでの時間である。体感速度を左右し、対話用途では1〜2秒以内が目安となる。
> **TPOT**（Time Per Output Token）は、1トークンを生成するのに要する時間である。文章が流れる速さに相当し、人間の読む速度である毎秒10〜20トークンが実用上の下限となる。
> いずれも平均値ではなく**p95（95%のリクエストが満たすべき値）**で目標設定することが重要である。

**机上計算のみで確定させないこと。** 特にMoEモデルは重みと実際の計算量の乖離が大きく、実測との差が出やすい。

---

## 付録: 用語一覧

| 用語 | 説明 |
| --- | --- |
| KVキャッシュ | 生成途中の中間結果を保持する領域。同時接続数の上限を決める |
| TP（テンソル並列） | 1モデルを複数GPUに横分割する方式。通信量が多くノード内限定 |
| PP（パイプライン並列） | 1モデルを層単位で縦分割する方式。通信量が少なくノード間向き |
| MoE | 内部を専門家に分割し一部のみ動作させる構造。パラメータ数の割に軽い |
| FP8 / BF16 | 数値の表現精度。FP8はBF16の半分のメモリで済む（量子化） |
| Prefill | 入力を読み込む段階。計算性能が律速 |
| Decode | 1トークンずつ生成する段階。メモリ帯域が律速 |
| TTFT | 最初の1文字が返るまでの時間。体感速度の指標 |
| TPOT | 1トークンあたりの生成時間。文章が流れる速さの指標 |
| RAG | 社内文書を検索し、その内容を含めて回答を生成する方式 |
| 埋め込みモデル | 文章をベクトル化し、意味の近さを計算可能にするモデル |
| リランカー | 検索候補を精密に照合し直して並べ替えるモデル |
| 拡散モデル | ノイズから段階的に画像を生成する方式。LLMとは計算特性が異なる |
| LoRA | 少数の追加パラメータのみを学習する軽量なファインチューニング手法 |
| vLLM | LLM配信用の推論エンジン。自社運用の事実上の標準 |
| レプリカ | 同一モデルの複製インスタンス。台数を増やして処理能力を拡張する |
| NVLink | GPU同士を直結する高速インターコネクト |
| MIG | 1枚のGPUをハードウェア的に区切り、複数ワークロードで排他利用する機能 |
